<div class="container">
  <h2>Iniciar sesión</h2>
  <div class="row">
    <div class="col-md-5 col-centered" align="left" style="font-size: 1.1em;">
    <%= bootstrap_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="field">
        <%= f.email_field :email, autofocus: true, label: "Correo electrónico" %>
      </div>

      <div class="field">
        <%= f.password_field :password, autocomplete: "off", label: "Contraseña" %>
      </div>

      <% if devise_mapping.rememberable? -%>
        <div class="field">
          <%= f.check_box :remember_me, label: "Recuerdame" %>
        </div>
      <% end -%>

      <div class="actions">
        <%= f.submit "Iniciar sesión", :class => 'btn btn-md btn-primary' %>
      </div>
    <% end %>
    </div>
  </div>
  <br>
  <%= render "devise/shared/links" %>
</div>
